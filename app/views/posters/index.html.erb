<div class="text-center">
  <span>
    <%= t("posters.total").capitalize %>: <%= @posters.count %>
  </span>
  <div>
    <%= link_to t("posters.new").capitalize, [:new, :poster] %>
  </div>
  <div>
    <% @posters.each do |poster| %>
      <%= link_to poster.title, poster %>
    <% end %>
  </div>
</div>
